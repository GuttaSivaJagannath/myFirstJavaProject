package com.multithreading;

public class SynchronizedAndSleepAndYeild5 {

	/*
	 * Sleep - This method causes the currently executing thread to sleep for the
	 * specified number of milliseconds, subject to the precision and accuracy of
	 * system timers and schedulers.
	 */

	/*
	 * Yeild - The yield() basically means that the thread is not doing anything
	 * particularly important and if any other threads or processes need to be run,
	 * they should run. Otherwise, the current thread will continue to run.
	 */

	/*
	 * Sy
	 */

}
